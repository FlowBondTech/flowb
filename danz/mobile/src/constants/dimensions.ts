// Consistent dimensions used across the app
export const DIMENSIONS = {
  coverImageHeight: 200, // Standard height for all cover images
  headerButtonPadding: 12, // Padding from status bar bottom
  headerButtonSize: 40, // Size of header action buttons
} as const
